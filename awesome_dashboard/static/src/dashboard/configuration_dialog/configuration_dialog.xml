<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.ConfigurationDialog">
        <Dialog title="title">
            <div class="d-flex flex-column gap-2">
                <t t-foreach="props.items" t-as="item" t-key="item.id">
                    <div class="form-check">
                        <input
                            type="checkbox"
                            class="form-check-input"
                            t-att-id="'item_' + item.id"
                            t-att-checked="!state.disabledItems.has(item.id)"
                            t-on-change="(ev) => this.onChange(item.id, ev.target.checked)"
                        />
                        <label class="form-check-label" t-att-for="'item_' + item.id">
                            <t t-esc="item.description || item.id"/>
                        </label>
                    </div>
                </t>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="apply">Apply</button>
                <button class="btn btn-secondary" t-on-click="props.close">Cancel</button>
            </t>
        </Dialog>
    </t>

</templates>